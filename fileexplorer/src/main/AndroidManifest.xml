<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.energy.fileexplorer" >
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/file_explorer"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <receiver android:enabled="true" android:label="SDCardMountReceiver" android:exported="true" android:name="com.energy.fileexplorer.File.ExternalStorage$ExternalStorageReceiver">
                <intent-filter>
                    <action android:name="android.intent.action.ACTION_MEDIA_REMOVED" />
                    <action android:name="android.intent.action.ACTION_MEDIA_UNMOUNTED." />
                    <action android:name="android.intent.action.ACTION_MEDIA_MOUNTED" />
                </intent-filter>
            </receiver>
        </activity>
    </application>

</manifest>
